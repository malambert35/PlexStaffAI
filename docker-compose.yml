services:
  plexstaffai:
    build: .
    container_name: plexstaffai
    ports:
      - "5056:5056"
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - media  # Your Overseerr/Plex network

networks:
  media:
    external: true
